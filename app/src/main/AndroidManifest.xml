<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.sumesh.android.diagonisticemr">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EMRActivity"
            android:label="@string/title_activity_emr"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EMR_select" />
        <activity android:name=".EMR_Diabetes_From" />
        <activity android:name=".EMR_Details" />
        <activity
            android:name=".Predict_disease_client"
            android:label="@string/title_activity_predict_disease_client"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>